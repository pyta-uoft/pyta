"""
AssignName astroid node

An assignment for a name node that appears in a Store (assignment) context.

Attributes:
    - name  (Optional[str])
        - The name which is assigned to.

Example:
    AssignName(name='x')
"""

x = 3
