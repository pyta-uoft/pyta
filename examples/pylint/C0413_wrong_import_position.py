"""pylint: wrong import position

Imports should be placed at the top of the module (below this docstring)....
When when code and imports are mixed.

"""

my_list = ['a', 'b']
import math  # imports should be at the top (below the docstring)
